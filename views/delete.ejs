<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delete User</title>
    <style>
      body {
        background: #f6f8fa;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 400px;
        margin: 70px auto;
        background: #fff;
        border-radius: 10px;
        box-shadow: 0 2px 16px rgba(0,0,0,0.08);
        padding: 36px 28px 28px 28px;
      }
      h3 {
        color: #d32f2f;
        text-align: center;
        font-weight: 600;
        margin-top: 0;
        margin-bottom: 32px;
      }
      form {
        display: flex;
        flex-direction: column;
        gap: 18px;
      }
      input[type="password"], input {
        padding: 12px;
        font-size: 16px;
        border: 1px solid #cfd8dc;
        border-radius: 5px;
        background: #f9fafb;
        transition: border 0.2s;
      }
      input:focus {
        outline: none;
        border: 1.5px solid #d32f2f;
        background: #fff;
      }
      button {
        background: #d32f2f;
        color: #fff;
        border: none;
        padding: 13px 0;
        border-radius: 5px;
        font-size: 17px;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.2s;
        margin-top: 10px;
      }
      button:hover {
        background: #a31515;
      }
      @media (max-width: 500px) {
        .container {
          padding: 18px 6px 14px 6px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h3>
        To delete this user: <%= user.email %><br>
        <span style="font-weight:400;color:#555;">Enter your password to confirm</span>
      </h3>
      <form method="post" action="/user/<%= user.id%>?_method=DELETE">
        <input
          type="password"
          placeholder="Enter password to delete"
          name="password"
          required
        />
        <button>Delete</button>
      </form>
    </div>
  </body>
</html>
